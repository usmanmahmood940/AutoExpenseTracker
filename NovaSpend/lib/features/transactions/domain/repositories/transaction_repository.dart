/// Transaction repository contract.
abstract class TransactionRepository {
  // Stream<List<Transaction>> watchTransactions(String userId);
  // Future<void> ingestManualTransaction({required String raw});
}
