# Copy to functions/.secret.local for local emulator testing (never commit)
# WEBHOOK_API_KEY=your-webhook-secret
# GEMINI_API_KEY=your-gemini-api-key

# Production secrets are set via Firebase Secret Manager:
#   firebase functions:secrets:set WEBHOOK_API_KEY
#   firebase functions:secrets:set GEMINI_API_KEY
